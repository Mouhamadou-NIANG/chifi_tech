import React from 'react';
import { Header } from '../../components'; 

const HomePage: React.FC = () => {
    return (
        <div>
            <Header />
            
        </div>
    );
};

export default HomePage;
